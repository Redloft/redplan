# Artifacts (kept in user's projects, not in this repo)
.plan-panel/
*.history/

# Feedback log — PRIVATE, may contain sensitive plan context
# Never commit even abstract analysis directly — use /panel-share-prompt
# which generates PR bundle with only role.md diff + abstract metrics
feedback/*.jsonl
feedback/_processed/

# Personal evolution / proposed changes
roles/_history/
roles/_proposed/

# macOS
.DS_Store

# Temp
*.bak
/tmp/
*.tmp

# Editor
.vscode/
.idea/
*.swp

# Node (in case anyone runs node helpers locally)
node_modules/
*.log
